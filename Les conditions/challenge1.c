#include <stdio.h>

int main() {
int N;
printf("enter the number:");
scanf("%d",&N);
if (N % 2 == 0) {
    printf("le nombre est pair");
}else{
    printf("le nombre est impaire");
}


    return 0;
}